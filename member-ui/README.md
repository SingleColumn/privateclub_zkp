# Member UI — Generate Proof Credential (mock)

Mock UI for the Member “Generate Proof Credential” screen. Built with React + Vite + Tailwind. Design: minimalist, dark theme, high-contrast accents.

## Run

```bash
npm install
npm run dev
```

Open the URL shown (e.g. http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```

## Component tree

See project root `COMPONENT_TREE.md` for the full component hierarchy and responsibilities.
